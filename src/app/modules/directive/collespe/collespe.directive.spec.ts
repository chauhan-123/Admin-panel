import { CollespeDirective } from './collespe.directive';

describe('CollespeDirective', () => {
  it('should create an instance', () => {
    const directive = new CollespeDirective();
    expect(directive).toBeTruthy();
  });
});
